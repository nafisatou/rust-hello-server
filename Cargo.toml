[package]
name = "rust-hello-server"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = { version = "0.7", features = ["multipart"] }  # ✅ Enable multipart
tokio = { version = "1", features = ["full"] }
tower-http = { version = "0.5", features = ["fs", "limit", "cors"] }

